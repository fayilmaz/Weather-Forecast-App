import {
  getWeatherState,
  getWeatherLoading,
  getWeatherLoaded,
  getWeatherLoadFail,
} from "./weather.selector";

const selectors = {
  getWeatherState,
  getWeatherLoading,
  getWeatherLoaded,
  getWeatherLoadFail,
};

export default selectors;
